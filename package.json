{
  "name": "fluxo-landing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/fix-static-paths.mjs",
    "preview": "astro preview --host 0.0.0.0"
  },
  "devDependencies": {
    "astro": "^6.4.2"
  }
}
